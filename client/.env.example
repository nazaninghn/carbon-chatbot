# ── Demo auto-login credentials ──────────────────────────────────────────────
# When the backend is first reached, the app registers/logs in with these
# credentials automatically (useful for demo/staging environments).
# If the backend is unreachable after retries, the real AuthPage is shown.
VITE_DEMO_EMAIL=demo@carboniq.app
VITE_DEMO_PASS=carboniq2024
VITE_DEMO_NAME=Demo User
